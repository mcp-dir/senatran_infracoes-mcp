# Ferramentas

SENATRAN: Infrações expõe 1 ferramenta (todas somente leitura).

### 1. `senatran_infracoes_consultar`
**Input**: `placa`, `cnpj` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `data_inicio` (opcional), `data_fim` (opcional)

SENATRAN: Infrações, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SENATRAN: Infrações
```
